# D1 Migrations

Wrangler-managed D1 migrations belong here.

This is Lantern's only checked-in D1 migration path. D1 uses SQLite, so
schema changes should preserve Lantern's trust boundary with intentional
SQLite-compatible SQL.
